// src/types/index.ts
export interface Contact {
    id: number;
    username: string;
    email: string;
    // Add other fields as necessary
  }

export interface addContact {
    username: string;
    email: string;
    // Add other fields as necessary
  }
  